apply plugin: "java"

group = "io.belov"
version "1.0"

repositories {
    mavenCentral()
}

dependencies {
    compile "org.slf4j:slf4j-api:1.7.12"
}

task wrapper(type: Wrapper) {
    gradleVersion = "2.3"
}